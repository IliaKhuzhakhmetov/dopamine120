name: sound_framework
description: "App-agnostic procedural sound framework with a SoLoud backend."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.5
  flutter: ">=3.3.0"

dependencies:
  audio_session: ^0.2.3
  flutter:
    sdk: flutter
  flutter_soloud: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3

flutter:
