name: core
description: "DOPAMINE120 shared infrastructure: DI, feature registrar, use case and key-value store contracts."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
